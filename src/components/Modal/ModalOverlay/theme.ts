import { ThemeComponentOptions } from "../../../theme";

const ModalOverlayOptions: ThemeComponentOptions = {
	default: {
		class: "top-0 left-0 fixed h-screen w-screen z-10",
	},
};

export default ModalOverlayOptions;
