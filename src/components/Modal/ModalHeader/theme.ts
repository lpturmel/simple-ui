import { ThemeComponentOptions } from "../../../theme";

const ModalHeaderOptions: ThemeComponentOptions = {
	default: {
		class: "flex flex-row justify-between px-4 py-4 text-xl font-semibold",
	},
};

export default ModalHeaderOptions;
